import 'package:flutter/material.dart';

class SunEight extends StatelessWidget {
  const SunEight({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}